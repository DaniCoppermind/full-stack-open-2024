{
  "name": "courseinfo",
  "version": "1.0.0",
  "description": "Full Stack 2024-2025",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rimraf dist && cd ../../../part2/notes-frontend && npm run build && copyfiles dist ../../../part3/courseInfo/backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Daniel García",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "nodemon": "^3.1.9",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
